# Shared skill GROUP definitions (the vocabulary projects compose from).
# Each project commits its own .skills.config that references these groups.
#   group <name> = <skill> [<skill> ...]

group core    = brainstorming systematic-debugging verification-before-completion writing-plans build-and-test
group runtime = target-runtime-exec runtime-target-config
